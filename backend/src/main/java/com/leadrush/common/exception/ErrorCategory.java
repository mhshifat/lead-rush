package com.leadrush.common.exception;

/** Error categories consumed by the frontend error handler. */
public enum ErrorCategory {
    VALIDATION,
    BUSINESS,
    AUTH,
    SYSTEM,
    ACTIVATION_REQUIRED
}
